fastapi==0.129.0
uvicorn==0.40.0
SQLAlchemy==2.0.46
psycopg2-binary==2.9.11
python-dotenv==1.2.1
python-jose==3.5.0
passlib[bcrypt]==1.7.4
pydantic==2.12.5
email-validator
# ---------- Core API ----------
fastapi==0.129.0
uvicorn==0.40.0

# ---------- Env ----------
python-dotenv==1.2.1

# ---------- Auth (JWT + hashing) ----------
python-jose==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator

# ---------- LLM (Groq) ----------
groq==1.0.0

# ---------- HTTP client (Maps + Wikipedia) ----------
httpx==0.27.2

# ---------- Speech-to-text (Meta MMS) ----------
transformers==4.49.0
torch>=2.1.0
soundfile==0.13.1
numpy==2.2.3

# Optional (only if you keep torchaudio-based loading somewhere)
# torchaudio>=2.1.0

# ---------- Images / poster utilities (you can remove pillow if not needed now) ----------
pillow==11.1.0